import { TokenStore } from '../token/TokenStore';
export declare function createAuthFetch(options?: {
    tokenStore?: TokenStore;
    defaultCredentials?: RequestCredentials;
    baseOrigin?: string;
}): typeof fetch;
