/**
 * 验证邮箱格式
 */
export declare function validateEmail(email: string): boolean;
/**
 * 验证密码强度
 * @param password 密码
 * @param minLength 最小长度，默认8
 */
export declare function validatePassword(password: string, minLength?: number): {
    valid: boolean;
    message?: string;
};
/**
 * 验证验证码格式
 * @param code 验证码
 * @param length 长度，默认6
 */
export declare function validateCode(code: string, length?: number): boolean;
/**
 * 验证姓名
 */
export declare function validateName(name: string): boolean;
/**
 * 检查两个密码是否匹配
 */
export declare function passwordsMatch(password: string, confirmPassword: string): boolean;
