import { ErrorMessageConfig } from '../core/types';
export declare class ErrorMessage {
    element: HTMLElement;
    private config;
    constructor(config: ErrorMessageConfig);
    private getClassName;
    setMessage(message: string, animate?: boolean): void;
    show(): void;
    hide(): void;
    setType(type: ErrorMessageConfig["type"]): void;
    destroy(): void;
}
