export interface LoadingPlaceholderOptions {
    container: HTMLElement;
}
/**
 * 骨架屏占位组件
 * 在配置加载期间显示，提供视觉反馈
 */
export declare class LoadingPlaceholder {
    private container;
    private root;
    constructor(opts: LoadingPlaceholderOptions);
    /**
     * 渲染骨架屏到容器
     */
    render(): void;
    /**
     * 从 DOM 中移除骨架屏
     */
    destroy(): void;
}
