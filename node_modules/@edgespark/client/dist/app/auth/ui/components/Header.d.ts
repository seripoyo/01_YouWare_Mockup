import { HeaderConfig } from '../core/types';
export declare class Header {
    private config;
    element: HTMLElement;
    constructor(config: HeaderConfig);
    private createLogo;
    private createSubtitle;
    update(config: Partial<HeaderConfig>): void;
    destroy(): void;
}
