export type ErrorCode = "CONFIG_NOT_FOUND" | "CONFIG_INVALID" | "NOT_INITIALIZED" | "PROVIDER_NOT_ENABLED" | "UI_INVALID_CONTAINER" | "BASE_URL_UNAVAILABLE";
export interface ErrorMeta {
    cause?: unknown;
    details?: Record<string, unknown>;
}
export declare class AuthError extends Error {
    code: ErrorCode;
    meta?: ErrorMeta;
    constructor(code: ErrorCode, message: string, meta?: ErrorMeta);
}
export declare const createError: (code: ErrorCode, message: string, meta?: ErrorMeta) => AuthError;
